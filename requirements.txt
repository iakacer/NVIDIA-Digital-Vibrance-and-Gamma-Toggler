customtkinter>=5.2.0
pystray>=0.19.5
Pillow>=9.5.0

# Build only. 6.22.2 is the first release that bundles Tcl/Tk 9.0 (shipped with
# Python 3.14 as libtcl9.x.zip rather than a loose tcl8.6/ directory); older
# PyInstaller silently bundles no Tcl data and the .exe dies on startup with
# 'Tcl data directory ... not found'.
pyinstaller>=6.22.2
